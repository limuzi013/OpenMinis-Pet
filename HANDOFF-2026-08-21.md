# OpenMinis Pet continuation handoff — 2026-08-21

This file is intentionally maintained as a live handoff so another coding agent can continue without
repeating the audit.

## Repository state

- Repository: `https://github.com/limuzi013/OpenMinis-Pet.git`
- Branch: `master`
- Last pushed commits:
  - `611edc0` — DSH-shaped WebSocket event-stream workbench
  - `fe616f0` — verified arm64 debug APK plus download/checksum documentation
- Published APK: `releases/OpenMinis-Pet-dsh-remote-rc8-arm64-debug.apk`
- APK SHA-256: `3dcc514ebded6f7d706b9d7e703ca0bc28002880e4ad8e247e6dba8cb1fb145f`
- APK size: `51,563,132` bytes
- Package: `dev.openminispet.android`; version `1.12-pet.11-SNAPSHOT` (versionCode 31)

## Current user priority

The Web Remote must have practical parity with the Android settings screens, not read-only cards.
The user specifically reproduced these gaps:

1. Session model cannot be switched and thinking level cannot be changed reliably.
2. Web cannot create/edit/delete Provider instances, API keys, or models.
3. Skills and MCP servers cannot be created/edited/updated; only some list/toggle/delete paths exist.
4. Environment variables are absent.
5. Shared folders (`/var/minis/{shared,skills,memory}`) and mounted external folders are absent.
6. Memory-file cards collapse into narrow, misaligned columns (screenshot supplied in the task).
7. Previously requested work remains: repair Android default-assistant role/wake path and continue the
   floating desktop pet toward a polished persistent Agent companion.

## Confirmed backend inventory

- Provider CRUD already exists in `debug/ProviderMutationMethods.kt` and is dispatched by
  `DebugRPCHandler`: `provider.instances.*`, `provider.models.*`, `provider.groups.*`.
- Web `/api/rpc` allows `provider.*` but deliberately denies `provider.export` and `provider.import`
  because those return stored secrets. Do not remove that deny rule.
- Session model/thinking REST routes already exist in `RemoteAccessServer.kt`:
  `/api/session/model` and `/api/session/thinking`.
- Skills currently expose only `list/get/toggle/delete` through `SkillRpcMethods`; the repository
  already supports `add`, `update`, GitHub import/update, file read/write, and archive import.
- MCP currently exposes only `list/toggle/delete` through `McpRpcMethods`; `MCPRepository` already
  supports `add`, `update`, import JSON, and reload.
- `EnvVarRepository` already supports list metadata plus add/update/delete; no RPC is currently wired.
- `MountedFoldersStore` supports list/rename/write-toggle/remove, but adding a mount requires an
  Android SAF picker and cannot honestly be completed from a browser. Web should list/manage existing
  mounts and deep-link/instruct the user to pick a new folder in the App.
- Shared folders are three fixed entries from `SharedFolderRegistry`: `/var/minis/shared` (R/W),
  `/var/minis/skills` (read-only in settings), `/var/minis/memory` (read-only in settings).

## Work in progress

- One parallel agent is implementing the default assistant fix. Confirmed root causes so far:
  `voice_interaction_service.xml` lacks `sessionService` and `recognitionService`, the session service
  uses the wrong bind permission, and role probing uses the wrong role string instead of
  `RoleManager.ROLE_ASSISTANT` (`android.app.role.ASSISTANT`).
- A desktop-pet agent is implementing a bounded P0 interaction/state iteration.
- A Web agent is updating `assets/remote/{app.js,app.css,index.html}` for the management surfaces and
  memory-card layout. Do not edit those three files concurrently until its result is merged.
- Root agent is adding missing Kotlin RPC methods for skills, MCP, environments, and folder inventory.

## Required verification before final push

From WSL:

```bash
export ANDROID_HOME=/home/liu-j/Android/Sdk
export ANDROID_SDK_ROOT=/home/liu-j/Android/Sdk
cd /mnt/c/Users/liu-j/Desktop/open/OpenMinis-pet-pi-web-v2-source-2026-08-20/src/android
./gradlew :app:testDebugUnitTest --no-daemon
./gradlew :app:assembleDebug --no-daemon
```

Windows adb currently used previously:

```text
C:\Users\liu-j\Documents\Codex\2026-08-16\new-chat\work\android-platform-tools\platform-tools\adb.exe
```

Install with `adb install -r <apk>`, verify package/version, launch
`com.openminis.app.MainActivityIconAuto`, then replace the tracked release APK and update its byte
count/SHA-256 in README, RELEASE-NOTES, BUILD-CN, and `releases/README.md`.

Always run `node --check src/android/app/src/main/assets/remote/app.js` and `git diff --check`.

## Safety and truthfulness constraints

- Web Remote is authenticated but may be tunnel-exposed. Never return Provider API keys or environment
  secret values in list endpoints. Accept write-only replacement values and return `hasCredential` /
  `hasValue` metadata.
- Do not pretend Web can invoke Android's SAF directory picker. Existing mounts can be listed/managed;
  creating one needs a native user gesture.
- Do not claim a full DeepSeek Harness runtime: the UI is source-adapted; Android `ChatViewModel` is the
  state authority.
- Preserve the existing `/api/rpc` deny list and workspace-write path policy.
